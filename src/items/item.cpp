// Processing magic level and skill boost
if (magic_level_check(STAT_MAGICPOINTS)) { 
    // Previous logic here
}

// Skill Boost processing
std::ostringstream ss;
if (magic_level_check(STAT_MAGICPOINTS)) {
    // Handle magic level
}
descriptions.emplace_back("Skill Boost", ss.str());

// Other existing logic continues here.