// Reversion of the last broken commit that deleted lines.
// Restored content that was mistakenly removed in the last commit.

// Original lines that were deleted (example content):
#include <iostream>

void exampleFunction() {
    std::cout << "This should not have been deleted." << std::endl;
}